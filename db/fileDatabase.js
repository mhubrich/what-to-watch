const fs = require("fs");
const Database = require("./database");

class FileDatabase extends Database {
    constructor(path) {
        this.path = path;
    }

    write() {

    }
    
    getAllRecords() {
        
    }

    addRecord(record) {
        
    }

    removeRecord(record) {
        
    }
}